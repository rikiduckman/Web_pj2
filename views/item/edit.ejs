<div class="d-flex justify-content-between flex-wrap flex-md nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2">Editing: <%= item.name %></h1>
    <div class="btn-toolbar mb-2 mb-md 0">
      <div class="btn-group me-2">
        <button class="btn btn-sm btn-outline-secondary">?</button>
      </div>
    </div>
  </div>
  <div class="col py-3">
    <div class="row">
      <div class="col">
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/addpage">All Product</a></li>
            <li class="breadcrumb-item active"> <%= item.name %></li>
          </ol>
        </nav>
      </div>
    </div>
    <div class="col text-end fw-lighter">
        <b>Last Updated: <%= new Date(item.updatedAt).toUTCString()  %></b>
        <b>ProductId:</b> <%= item._id %>
      </div>
  
  </div>
  
        <div class="modal-body">
          <form method="post" action="/edit/<%= item._id %>?_method=PUT" >
            <div class="mb-3">
                <label for="exampleInputName" class="form-label">Name Product</label>
                <input type="name"name="name" class="form-control"value="<%= item.name %>" required>
            </div>
                <label for="exampleInputName" class="form-label">Price Product</label>
            <div class="input-group mb-3">
            <span class="input-group-text" id="basic-addon1">$</span>
                <input type="text"name="price" class="form-control"value="<%= item.price %>" required>
            </div>
                <label for="exampleInputName" class="form-label">Type Product</label>
                    <select class="form-select" aria-label="Default select example" name="category"value="<%= item.category %>"  required>
                        <option selected><%= item.category %></option>
                        <option value="เสื้อ">เสื้อ</option>
                        <option value="กางเกง">กางเกง</option>
                        <option value="รองเท้า">รองเท้า</option>
                    </select>
            <label for="exampleInputName" class="form-label mt-3">Details Product</label>
            <div class="form-floating">
                <textarea class="form-control"name="details" placeholder="Leave a comment here"><%= item.details %></textarea>
                <label for="floatingTextarea">Details</label>
            </div>
                <button type="submit" class="btn btn-primary mt-3 ">อัพเดทสินค้า</button>
                <button type="button" class="btn btn-danger mt-3" data-bs-toggle="modal" id="deleteButton" data-bs-target="#deleteModal">ลบสินค้า</button>
            </form>
          </div>



          <div class="modal fade" tabindex="-1" role="dialog" id="deleteModal">
            <div class="modal-dialog" role="document">
              <div class="modal-content">
                <div class="modal-header">
                  <div class="modal-title">ทำรายการลบสินค้า</div>
                  <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                  <p>
                    คุณแน่ใจไหมว่าจะลบสินค้า ชื่อ <b class="fw-bold"><%= item.name %></b><br/>
                  </p>
                </div>
                <div class="modal-footer">
                  <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                  <form action="/edit/<%= item._id %>?_method=DELETE" method="POST" class="position-relative">
                    <button type="submit" class="btn btn-primary">ยืนยัน</button>
                  </form>
                </div>
              </div>
            </div>
          </div>